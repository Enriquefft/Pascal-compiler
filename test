program RecordPointer;
type
  Rec = record name:string; value:real; end;
var
  p:^Rec;
begin
  new(p);
  p^.name := 'Pi';
  p^.value := 3.14;
  writeln(p^.name, ': ', p^.value:0:2);
  dispose(p);
end.
